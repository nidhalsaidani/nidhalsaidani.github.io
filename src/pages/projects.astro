---
import Layout from '../layouts/Layout.astro';
import { getCollection } from 'astro:content';

const allProjects = await getCollection('projects');
const featuredProjects = allProjects.filter(({ data }) => data.featured);
const otherProjects = allProjects.filter(({ data }) => !data.featured);
---

<Layout title="Projects" description="Research and personal projects">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-16">
    <h1 class="text-4xl font-bold font-serif text-gray-900 dark:text-gray-100 mb-4">
      Projects
    </h1>
    <p class="text-lg text-gray-600 dark:text-gray-400 mb-12">
      A collection of research, software, and collaborative projects
    </p>
    
    <!-- Featured Projects -->
    {featuredProjects.length > 0 && (
      <div class="mb-16">
        <h2 class="text-2xl font-semibold text-gray-900 dark:text-gray-100 mb-6">
          Featured Projects
        </h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {featuredProjects.map((project) => (
            <article class="card flex flex-col h-full">
              <div class="flex-grow">
                <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-2">
                  {project.data.title}
                </h3>
                <p class="text-gray-600 dark:text-gray-400 mb-4">
                  {project.data.description}
                </p>
                {project.data.tags && project.data.tags.length > 0 && (
                  <div class="flex flex-wrap gap-2 mb-4">
                    {project.data.tags.map((tag) => (
                      <span class="px-2 py-1 bg-gray-100 dark:bg-gray-800 rounded text-xs text-gray-600 dark:text-gray-400">
                        {tag}
                      </span>
                    ))}
                  </div>
                )}
              </div>
              <div class="flex items-center gap-4 pt-4 border-t border-gray-200 dark:border-dark-border">
                {project.data.link && (
                  <a
                    href={project.data.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-sm font-medium text-primary-600 dark:text-primary-400 hover:underline"
                  >
                    Visit Project →
                  </a>
                )}
                {project.data.github && (
                  <a
                    href={project.data.github}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-sm font-medium text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100"
                  >
                    <svg class="w-5 h5" fill="currentColor" viewBox="0 0 24 24">
                      <path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/>
                    </svg>
                  </a>
                )}
              <span class="text-sm text-gray-500 dark:text-gray-400 ml-auto">
                  {new Date(project.data.date).getFullYear()}
                </span>
              </div>
            </article>
          ))}
        </div>
      </div>
    )}
    
    <!-- Other Projects -->
    {(otherProjects.length > 0 || featuredProjects.length === 0) && (
      <div>
        {featuredProjects.length > 0 && (
          <h2 class="text-2xl font-semibold text-gray-900 dark:text-gray-100 mb-6">
            Other Projects
          </h2>
        )}
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          {(featuredProjects.length > 0 ? otherProjects : allProjects).map((project) => (
            <article class="card">
              <h3 class="text-xl font-semibold text-gray-900 dark:text-gray-100 mb-2">
                {project.data.title}
              </h3>
              <p class="text-gray-600 dark:text-gray-400 mb-4">
                {project.data.description}
              </p>
              <div class="flex items-center gap-4">
                {project.data.link && (
                  <a
                    href={project.data.link}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-sm font-medium text-primary-600 dark:text-primary-400 hover:underline"
                  >
                    Visit →
                  </a>
                )}
                {project.data.github && (
                  <a
                    href={project.data.github}
                    target="_blank"
                    rel="noopener noreferrer"
                    class="text-sm font-medium text-gray-600 dark:text-gray-400 hover:text-gray-900 dark:hover:text-gray-100"
                  >
                    Code →
                  </a>
                )}
                <span class="text-sm text-gray-500 dark:text-gray-400 ml-auto">
                  {new Date(project.data.date).toLocaleDateString()}
                </span>
              </div>
            </article>
          ))}
        </div>
      </div>
    )}
    
    {allProjects.length === 0 && (
      <div class="text-center py-12">
        <p class="text-gray-500 dark:text-gray-400">
          No projects yet. Check back soon!
        </p>
      </div>
    )}
  </div>
</Layout>
